// <your-demo> — TEMPLATE entry point.
//
// Replace this file with your demo. The house style is a single, heavily
// commented translation unit that walks the reader through the Pulse calls it
// showcases. See demos/doppler/src/main.cpp for the canonical example, and
// demos/pexninja/pexninja.cpp for the widest range of Pulse usage.
//
// The Pulse C API headers are under <pexpulse/...>:
//
//     #include <pexpulse/pulse.h>
//
// A minimal skeleton:
//
//     Pulse * p = pulse_new();
//     // pulse_options_set_*(p, ...);   // callbacks, window handles, ...
//     // pulse_connect_with_rest_async(p, ...);
//     // ... your demo ...
//     // pulse_disconnect_async(p);
//     pulse_free(p);

int main()
{
    return 0;
}
